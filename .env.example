# ============================================================================
# ALTCHA Server Configuration
# ============================================================================

# REQUIRED: Your HMAC secret key (min. 32 characters recommended)
# Generate with: openssl rand -hex 32
ALTCHA_HMAC_KEY=your-super-secret-key-min-32-chars-recommended-use-openssl-rand

# Server Configuration
PORT=8080

# ALTCHA Challenge Configuration
ALTCHA_MAX_NUMBER=100000          # Maximum number for PoW calculation
ALTCHA_SALT_LENGTH=12             # Length of random salt
ALTCHA_EXPIRES_MINUTES=5          # Challenge expiration time in minutes

# CORS Configuration
CORS_ORIGIN=*                     # Allow all origins (use specific domain in production)
